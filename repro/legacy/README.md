# Historical verifier

`verify_spectra_rejected.py` and `spectra_rejected.py` are preserved for
provenance only. They used a toy K=2/D=5 setup, did not implement the paper’s
faithful six-claim protocol, and are excluded from `publication_gate.json`.

Use [`../verify_all.py`](../verify_all.py) for the canonical reproduction.
